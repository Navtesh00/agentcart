# Razorpay Test Mode — real Orders order_* (rzp_test_TXPaQPvvVu85mH)
RAZORPAY_KEY_ID=rzp_test_TXPaQPvvVu85mH
RAZORPAY_KEY_SECRET=msCBFS4lHA2VLffUW0wfjijp
RAZORPAY_WEBHOOK_SECRET=whsec_test_dummy
PORT=3001
WEB_URL=http://localhost:5173
MCP_PORT=3002
RESERVE_MAX_BLOCK=10000
RESERVE_VALID_DAYS=90
# Agent-facing API auth: per-agent HMAC secrets (agent_id:secret, comma-separated).
# Requests must include x-agent-id, x-nonce, x-timestamp, x-signature:
#   signature = HMAC-SHA256(secret, METHOD + PATH + TIMESTAMP + NONCE + BODY)
# Secrets are only on the server + MCP server (never in /llms.txt or headers).
AGENT_SECRETS=agent_demo:demo_agent_secret_change_me_01

DATABASE_URL=postgresql://user:pass@localhost:5432/agentcart
DATABASE_URL=postgresql://postgres:password@localhost:5432/agentcart
